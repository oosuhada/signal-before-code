# 04 — Stack & Queue

**v0.1 state: scaffold only. Do not treat this chapter as learned or complete.**

No full lesson is generated yet.

## Learning question to earn the chapter

What does the problem require about **which unresolved item must be processed next**?

Signals to investigate through real attempts, not memorize from this scaffold:

```text
most recent unresolved state
→ stack candidate

oldest waiting state / distance frontier
→ queue candidate
```

The chapter should eventually distinguish the abstract ordering rule from Python's concrete list or
`deque` APIs, and it must include cases where neither structure is necessary.

Use the curated candidates in [`../../curriculum/problems.json`](../../curriculum/problems.json) when
the study session reaches this pattern.
