# 05 — Binary Search

**v0.1 state: scaffold only. Do not treat this chapter as learned or complete.**

No full explanation or solution code is generated yet.

## Learning question to earn the chapter

What property lets one comparison eliminate **half of the remaining search space**?

The future chapter must separately derive:

- search over sorted data;
- search over a monotonic answer predicate;
- why unordered arbitrary search does not qualify;
- boundary conventions and off-by-one reasoning;
- a mutation where monotonicity disappears.

The problem pool is in [`../../curriculum/problems.json`](../../curriculum/problems.json).
